#!/usr/bin/node

/*
 * A script that prints output in three lines
 */

const first = 'C is fun';
const second = 'Python is cool';
const third = 'JavaScript is amazing';

console.log(first + '\n' + second + '\n' + third);
